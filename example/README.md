# AH Flutter SDK - Example App

A minimal Flutter app demonstrating how to use `ah_flutter_sdk` to connect to a voice bot.

## Running

1. Start the backend server: `cd ../server && uvicorn main:app --reload`
2. Run the app: `flutter run`
