name: example
description: "Example app for ah_daily_flutter_sdk."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  ah_daily_flutter_sdk:
    path: ../
  http: ^1.6.0
  permission_handler: ^11.4.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
